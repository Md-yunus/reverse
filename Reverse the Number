#include <stdio.h>

int main()
{
    long long int num;
    scanf("%lld",&num);
    int rev,rem=0;
    while(num>0){
        rem=num%10;
        rev=rev*10+rem;
        num=num/10;
    }
    printf("%d",rev);

    return 0;
}
